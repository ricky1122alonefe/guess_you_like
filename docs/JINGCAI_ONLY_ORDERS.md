# 已作废（2026-08-10）

本文曾指令「交付 `fussball-bund`、`guess_you_like` 只读对照」。
该方向 **已作废**：产品回归 **`guess_you_like`**，`fussball-bund` 停止产品改造。

现行唯一范围指令：

→ [`docs/PRODUCT_SCOPE.md`](./PRODUCT_SCOPE.md)

要点：
- 主路径：`poll --days 7` → `serve :8765` → 竞彩列表/详情
- P0：首页竞彩优先过滤 + poll 状态可见
- 禁止：两边同时加功能、再救 bund Web
